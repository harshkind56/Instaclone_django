from django.apps import AppConfig


class ContentConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'content'


def ready(self):
    import content
    # flow of code and understanding each concepts.
    #database queries complex and simple/.
    #more profiecent in python, more python cources.
    # more concepts building in python.
    #code work flow.